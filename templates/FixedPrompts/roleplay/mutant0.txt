python
import numpy as np

def predict(X):
    # Predict using a simple linear regression model
    w = 2
    b = 3
    y_pred = w * X + b
    return y_pred

# Example usage
X_train = np.array([1, 2, 3, 4, 5])
y_train = np.array([5, 7, 9, 11, 13])

X_test = np.array([6, 7, 8, 9, 10])
y_test = np.array([15, 17, 19, 21, 23])

print(predict(X_train))
print(predict(X_test))

```python
{}
```